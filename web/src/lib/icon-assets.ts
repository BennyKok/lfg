// Bump this URL whenever the small mark changes. The query is intentionally
// immutable on the server, so Safari, an installed PWA, and the service worker
// all agree that different artwork has a different cache key.
export const LFG_SMALL_ICON_PATH = "/icon-small.svg?v=20260731-vector";
